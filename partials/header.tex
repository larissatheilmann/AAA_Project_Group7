% Indentation
\usepackage{indentfirst}
\setlength{\parindent}{30pt}

% Multi-row table cells (used by embedded pandas/great-tables output)
\usepackage{multirow}

% Keep wide auto-generated tables (embedded pandas DataFrames) within the text
% margin. longtable holds the many-column numeric tables (mostly in the
% appendix); a smaller font plus tighter inter-column padding reclaims the
% horizontal space so their content no longer spills past the right margin.
% The font is set *before* the environment so column widths are measured at that
% size -- an \AtBeginEnvironment hook is reset by longtable and has no effect.
% Text-heavy tables carry explicit column widths and still wrap normally.
\usepackage{etoolbox}
\setlength{\tabcolsep}{4pt}
% Default font for longtables; a few extra-wide tables lower it to \scriptsize
% by \renewcommand-ing \longtablesize just before their embed (see the appendix).
\newcommand{\longtablesize}{\footnotesize}
\BeforeBeginEnvironment{longtable}{\setlength{\tabcolsep}{3pt}\longtablesize}
\BeforeBeginEnvironment{tabular}{\small}
% Allow long monospace tokens (e.g. notebook filenames) to break across lines
% instead of protruding into the margin.
\usepackage[htt]{hyphenat}

% Section titles formatting
\usepackage{titlesec}
\titleformat{\section}
    {\normalfont\bfseries\fontsize{14}{20}\selectfont}
    {\thesection}{1em}{}
\titlespacing*{\section}
    {0pt}{10pt}{0pt}

\titleformat{\subsection}
    {\normalfont\bfseries\fontsize{13}{20}\selectfont}
    {\thesubsection}{1em}{}
\titlespacing*{\subsection}
    {0pt}{10pt}{0pt}

\titleformat{\subsubsection}
    {\normalfont\bfseries\fontsize{12}{20}\selectfont}
    {\thesubsubsection}{1em}{}
\titlespacing*{\subsubsection}
    {0pt}{10pt}{0pt}

\titleformat{\paragraph}
    {\normalfont\bfseries\fontsize{11}{20}\selectfont}
    {\theparagraph}{1em}{}
\titlespacing*{\paragraph}
    {0pt}{10pt}{0pt}

% Headers and Footers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead{}
\fancyfoot{}
\fancyhead[L]{\textsl{\leftmark}}
\fancyhead[R]{\headerauthor}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% Define placeholders for metadata used in header/footer
\providecommand{\headerauthor}{}
